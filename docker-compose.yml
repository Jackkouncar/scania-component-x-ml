services:
  scania-dashboard:
    build: .
    ports:
      - "5173:5173"
    environment:
      PORT: 5173
      APP_DB_FILE: /app/database/eneb453_app.sqlite
      DEMO_ADMIN_USERNAME: demoadmin
      DEMO_USER_USERNAME: demouser
      DEMO_PASSWORD: ComponentX453!
      SESSION_SECRET: change-this-secret-before-public-deployment
    volumes:
      - scania_dashboard_db:/app/database

volumes:
  scania_dashboard_db:
